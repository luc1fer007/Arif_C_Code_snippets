#include<stdio.h>

int main() {
    float length;
    printf("Enter the value of length : ");
    scanf("%f",&length);

    float area =6*(length*length);
    printf("The area of a cube : %f \n",area);

    return 0;
}